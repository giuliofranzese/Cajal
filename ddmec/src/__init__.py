"""
Local `src` package for the standalone `cajal/` copy.

This package mirrors the original repository's `src` package but lives
inside `cajal/` so the notebook can run standalone without importing
code from the parent repo.
"""
